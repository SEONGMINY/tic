[project]
name = "draglab"
version = "0.1.0"
description = "Python drag-and-drop experiment harness for tic calendar gestures"
requires-python = ">=3.9"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
